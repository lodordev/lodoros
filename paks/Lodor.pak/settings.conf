# Optional runtime overrides for the Lodor pak, written by the in-launcher
# Settings tool. Safe to leave empty — sane defaults apply when a key is absent.
# Wi-Fi is simply kept up while in use (no keep-warm/grace knobs anymore); any
# stale keys here are ignored.
