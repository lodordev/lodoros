Build-tree launcher source snapshot 2026-06-30 — the AUTHORITATIVE superset that builds the working all-platform minui.elf (reproduces rg35xxplus 79a57f10 byte-for-byte). Source lives in a volatile local build workspace. This is the backup. #60 = fold this into lodoros/launcher/ as the one canonical tree (the canonical tree currently differs: separate cheevos.c/minarch structure). minui.c here is the SUPERSET (multi-user + RA + kbd + bg-covers + tier-dots + box-art + version + continue-fix).
